# Express App

A small Express + TypeScript app with Docker support.

## Endpoints

- `GET /` — returns a simple hello message
- `GET /messages` — returns the same hello payload
- `POST /messages/create` — saves a message body to `data/messages.txt`

## Run

```bash
npm install
npm run dev
```
